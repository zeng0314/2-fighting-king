# 吵架大王项目运行指南

## 🚀 运行方式

### 方式一：终端命令运行
```bash
# 1. 安装依赖
pnpm install

# 2. 启动开发服务器
pnpm dev
# 或
pnpm dev:client

# 3. 在浏览器打开
# 服务器将在 http://localhost:3000 启动
```

### 方式二：VSCode调试
1. 确保已安装推荐扩展：
   - JavaScript Debugger (内置)
   - React相关扩展

2. 使用终端运行开发服务器：
   ```bash
   pnpm dev
   ```

3. 在VSCode中按 `F5` 选择 Chrome 调试配置

## 🔍 LLM调用详情查看

### 当前实现状态
⚠️ **重要提示**: 当前项目使用的是**模拟LLM实现**，并非真实调用外部API。

### 查看调用详情的方法

#### 1. 浏览器开发者工具
- 按 `F12` 打开开发者工具
- 切换到 `Console` 标签页
- 所有LLM调用会在控制台输出详细信息

#### 2. 代码调试
在需要调试的组件中添加：
```typescript
import { logLLMRequest, logLLMResponse } from '@/lib/llmService-debug';

// 在调用LLM前
logLLMRequest(request, type);

// 在收到响应后
logLLMResponse(response, processingTime);
```

#### 3. 网络请求监控
如果将来接入真实LLM API，可以在：
- 开发者工具 → Network 标签页
- 筛选 `XHR` 请求
- 查看请求/响应详情

## 📊 LLM调用传递链

### 当前模拟调用流程：
```
用户输入 → 场景选择 → 语气选择 → 
本地模拟LLM服务 → 生成响应 → 界面展示
```

### 预期真实调用流程：
```
用户输入 → 场景/语气参数 → API请求 → 
外部LLM服务 → JSON响应 → 解析展示
```

## 🛠️ 接入真实LLM服务

如需接入真实LLM（如OpenAI），需要：

1. 安装相关SDK：
```bash
pnpm add openai
```

2. 修改 `src/lib/llmService.ts`：
```typescript
import OpenAI from 'openai';

const openai = new OpenAI({
  apiKey: process.env.OPENAI_API_KEY,
});

// 替换模拟调用为真实API调用
```

3. 添加环境变量文件 `.env`：
```
VITE_OPENAI_API_KEY=your_api_key_here
```

## 📝 调试技巧

### 实时日志查看
```typescript
// 在组件中添加
useEffect(() => {
  console.log('当前LLM请求参数:', requestParams);
}, [requestParams]);
```

### 性能监控
```typescript
// 测量LLM调用性能
const start = performance.now();
const response = await generateLLMResponse(request);
const duration = performance.now() - start;
console.log(`LLM调用耗时: ${duration}ms`);
```

## 🔧 故障排除

### 常见问题
1. **端口占用**：修改 `vite.config.ts` 中的端口号
2. **依赖问题**：删除 `node_modules` 和 `pnpm-lock.yaml`，重新安装
3. **API密钥**：确保正确设置环境变量

### 获取帮助
查看项目文档或联系开发团队获得技术支持。